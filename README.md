# OrfinSupport playground

Built static demo for [OrfinSupport](https://github.com/arconw/OrfinSupport).

[Open the playground](https://arconw.github.io/OrfinSupport/)

Library and editable demo source: [`main`](https://github.com/arconw/OrfinSupport/tree/main).

Source revision: `3e4edae006d314dd8f9dae1d83602821b5a58178`.

This public playground uses localized sample replies with working catalog, report, comparison, review and cart tools. Live AI is disabled here; run the source locally to connect your LLM backend. No purchases or payments are available.

The assistant supports 16 languages, with English as the default. Change Language in assistant preferences or in the Playground settings.

Ten assistant themes, a full host-styled mode, a configurable logo and soft section highlights are available in the Playground. Reports and Equipment demonstrate data analysis and real local interface actions.

The labelled Actions menu stays below the chat composer. Panel, menu, message, tool and settings motion respects device preferences and can be disabled at runtime. Orfin is a rounded, scalloped vector character with friendly ring eyes and a small smile. Both expressions use larger facial details for readability at 16–40 pixels, preserving the original silhouette. Its face smoothly becomes happy closed eyes during actual waiting, tools and streamed replies, then returns on completion, Stop or error. Reduced motion switches expressions immediately. Static branding uses the smiling face; supplied project images stay unchanged.

Project commands are configurable and route-specific: Equipment offers Compare products, Reports offers Analyze delivery. Playground can reorder, restore or hide the menu. Waiting, writing and tool activity follow the real response lifecycle; Stop marks unfinished activity as interrupted.

Copy configuration exports usable command arrays with declarative route rules and localized visitor prompts. Project styles includes the actual CSS. Internal tool selection is handled by the project backend.

The workspace and assistant use locally hosted Inter with firmer strokes and stronger text contrast. Messages use 15px type and the composer uses 16px. The project-owned serif appearance remains independent. Documentation recordings capture the current demo at native resolution.
